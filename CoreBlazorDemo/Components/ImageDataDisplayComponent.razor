@using CoreBlazor.Configuration
@using CoreBlazor.Interfaces
@using DemoDb
@implements IEntityDisplayComponent<ImageData>
<ImageDisplayComponent PropertyValue="@Entity?.Data"/>
@code {

    [Parameter]
    public ImageData? Entity { get; set; }
}
